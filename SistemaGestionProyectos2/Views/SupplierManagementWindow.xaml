<Window x:Class="SistemaGestionProyectos2.Views.SupplierManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Proveedores" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal">

    <Window.Resources>
        <!-- Colores consistentes con el proyecto -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="PrimaryLight" Color="#E0E7FF"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessLight" Color="#D1FAE5"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerLight" Color="#FEE2E2"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="WarningLight" Color="#FEF3C7"/>
        <SolidColorBrush x:Key="InfoColor" Color="#3B82F6"/>
        <SolidColorBrush x:Key="InfoLight" Color="#DBEAFE"/>
        <SolidColorBrush x:Key="BackgroundGray" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="BorderGray" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextGray" Color="#6B7280"/>
        <SolidColorBrush x:Key="DarkText" Color="#111827"/>

        <!-- Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Estilo base para tarjetas de proveedor -->
        <Style x:Key="SupplierCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" Direction="270" 
                                    ShadowDepth="1" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="12" Direction="270" 
                                            ShadowDepth="2" Opacity="0.1"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryLight}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Estilo para botones modernos -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" Direction="270" 
                                                ShadowDepth="1" Opacity="0.1"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones secundarios -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundGray}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones de acción pequeños -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="500"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundGray}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header compacto -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderGray}" 
                BorderThickness="0,0,0,1">
            <Grid Margin="24,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="GESTIÓN DE PROVEEDORES" 
                         FontSize="20" 
                         FontWeight="Bold" 
                         Foreground="{StaticResource DarkText}"
                         VerticalAlignment="Center"/>

                <Button Grid.Column="1" 
                        Name="CloseButton"
                        Content="← Volver" 
                        Style="{StaticResource SecondaryButton}"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Barra de herramientas -->
        <Grid Grid.Row="1" Margin="24,12,24,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Búsqueda y filtros -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <!-- Búsqueda -->
                <Border Background="White" CornerRadius="6" Width="280" 
                        BorderThickness="1" BorderBrush="{StaticResource BorderGray}">
                    <Grid>
                        <TextBox Name="SearchBox"
                               BorderThickness="0"
                               Background="Transparent"
                               Padding="32,8,8,8"
                               FontSize="13"
                               VerticalAlignment="Center"
                               TextChanged="SearchBox_TextChanged"/>
                        <TextBlock Text="🔍" 
                                 Margin="10,0,0,0" 
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextGray}"
                                 IsHitTestVisible="False"/>
                        <TextBlock Text="Buscar proveedor..." 
                                 Margin="32,0,0,0"
                                 VerticalAlignment="Center"
                                 Foreground="{StaticResource TextGray}"
                                 IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- Filtro Estado -->
                <ComboBox Name="StatusFilterCombo"
                        Width="120"
                        Height="34"
                        Margin="12,0,0,0"
                        FontSize="13"
                        SelectionChanged="StatusFilterCombo_SelectionChanged">
                    <ComboBoxItem IsSelected="True">Todos</ComboBoxItem>
                    <ComboBoxItem>Activos</ComboBoxItem>
                    <ComboBoxItem>Inactivos</ComboBoxItem>
                </ComboBox>
            </StackPanel>

            <!-- Botón nuevo -->
            <Button Grid.Column="2" Name="NewSupplierButton"
                    Style="{StaticResource ModernButton}"
                    Click="NewSupplierButton_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="➕" FontSize="12" Margin="0,0,6,0"/>
                    <TextBlock Text="Nuevo Proveedor"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Lista de proveedores con tarjetas -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                      Margin="24,0,24,12">
            <ItemsControl Name="SuppliersItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="16,12">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource SupplierCard}">
                                    <Style.Triggers>
                                        <!-- Si es nuevo -->
                                        <DataTrigger Binding="{Binding IsNew}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#F0FDF4" Offset="0"/>
                                                        <GradientStop Color="#E8F5E9" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect Color="#10B981" BlurRadius="16" Direction="270" 
                                                                    ShadowDepth="3" Opacity="0.3"/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>

                                        <!-- Si está siendo editado (pero no es nuevo) -->
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsEditing}" Value="True"/>
                                                <Condition Binding="{Binding IsNew}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                        <GradientStop Color="#F0F4FF" Offset="0"/>
                                                        <GradientStop Color="#E8EEFF" Offset="1"/>
                                                    </LinearGradientBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Badge NUEVO/EDITANDO -->
                                <Border Grid.ColumnSpan="7" HorizontalAlignment="Right" VerticalAlignment="Top" 
                                        Margin="0,-8,-8,0"
                                        CornerRadius="8" Padding="6,3">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
                                                </DataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsEditing}" Value="True"/>
                                                        <Condition Binding="{Binding IsNew}" Value="False"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="10" FontWeight="Bold" Foreground="White">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="EDITANDO"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                        <Setter Property="Text" Value="NUEVO"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>

                                <!-- Ícono -->
                                <Border Grid.Column="0" Width="40" Height="40" 
                                        CornerRadius="20" Margin="0,0,12,0"
                                        Background="{StaticResource PrimaryLight}">
                                    <TextBlock Text="🏢" FontSize="20"
                                             HorizontalAlignment="Center" 
                                             VerticalAlignment="Center"/>
                                </Border>

                                <!-- Nombre y estado -->
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <Grid>
                                        <TextBox Name="NameTextBox"
               Text="{Binding SupplierName, UpdateSourceTrigger=PropertyChanged}" 
               FontSize="14" FontWeight="SemiBold"
               Foreground="{StaticResource DarkText}"
               BorderThickness="0"
               Background="Transparent"
               IsReadOnly="True"
               GotFocus="Field_GotFocus"
               LostFocus="Field_LostFocus"
               KeyDown="Field_KeyDown"/>
                                        <TextBlock Text="Nombre del proveedor *" 
                 FontSize="11"
                 Foreground="{StaticResource TextGray}"
                 IsHitTestVisible="False"
                 Margin="2,0,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding Text.Length, ElementName=NameTextBox}" Value="0"/>
                                                                <Condition Binding="{Binding IsEditing}" Value="True"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <Border Background="{StaticResource SuccessLight}"
                                                CornerRadius="4" Padding="6,2"
                                                Visibility="{Binding IsActive, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="ACTIVO" FontSize="10" FontWeight="Bold"
                                                     Foreground="{StaticResource SuccessColor}"/>
                                        </Border>
                                        <Border Background="{StaticResource DangerLight}"
                                                CornerRadius="4" Padding="6,2"
                                                Visibility="{Binding IsInactive, Converter={StaticResource BoolToVisibilityConverter}}">
                                            <TextBlock Text="INACTIVO" FontSize="10" FontWeight="Bold"
                                                     Foreground="{StaticResource DangerColor}"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>

                                <!-- RFC -->
                                <Border Grid.Column="2" VerticalAlignment="Center" Margin="12,0">
                                    <StackPanel>
                                        <TextBlock Text="RFC" FontSize="10" 
                                                 Foreground="{StaticResource TextGray}"
                                                 Margin="0,0,0,2"/>
                                        <TextBox Text="{Binding TaxId, UpdateSourceTrigger=PropertyChanged}"
                                               FontSize="12" FontWeight="Medium"
                                               BorderThickness="0"
                                               Background="Transparent"
                                               IsReadOnly="True"
                                               GotFocus="Field_GotFocus"
                                               LostFocus="Field_LostFocus"
                                               KeyDown="Field_KeyDown"/>
                                    </StackPanel>
                                </Border>

                                <!-- Contacto -->
                                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="0,0,12,0">
                                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                                           FontSize="11" 
                                           Foreground="{StaticResource TextGray}"
                                           BorderThickness="0"
                                           Background="Transparent"
                                           IsReadOnly="True"
                                           GotFocus="Field_GotFocus"
                                           LostFocus="Field_LostFocus"
                                           KeyDown="Field_KeyDown"/>
                                    <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" 
                                           FontSize="11" 
                                           Foreground="{StaticResource TextGray}"
                                           BorderThickness="0"
                                           Background="Transparent"
                                           IsReadOnly="True"
                                           Margin="0,2,0,0"
                                           GotFocus="Field_GotFocus"
                                           LostFocus="Field_LostFocus"
                                           KeyDown="Field_KeyDown"/>
                                </StackPanel>

                                <!-- Dirección -->
                                <TextBox Grid.Column="4" 
                                       Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"
                                       FontSize="12"
                                       Foreground="{StaticResource TextGray}"
                                       BorderThickness="0"
                                       Background="Transparent"
                                       IsReadOnly="True"
                                       TextWrapping="Wrap"
                                       VerticalAlignment="Center"
                                       Margin="0,0,12,0"
                                       GotFocus="Field_GotFocus"
                                       LostFocus="Field_LostFocus"
                                       KeyDown="Field_KeyDown"/>

                                <!-- Días de crédito editable inline -->
                                <Border Grid.Column="5" 
                                        BorderBrush="{StaticResource BorderGray}"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Background="White">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0" Text="📅" 
                                                 FontSize="12" Margin="8,0,2,0"
                                                 VerticalAlignment="Center"/>

                                        <TextBox Grid.Column="1"
                                               Text="{Binding CreditDays, UpdateSourceTrigger=PropertyChanged}"
                                               FontSize="13" 
                                               FontWeight="Bold"
                                               Background="Transparent" 
                                               BorderThickness="0"
                                               TextAlignment="Center"
                                               VerticalAlignment="Center"
                                               PreviewTextInput="CreditDays_PreviewTextInput"
                                               PreviewMouseDown="CreditDays_PreviewMouseDown"
                                               KeyDown="CreditDays_KeyDown"
                                               LostFocus="CreditDays_LostFocus"
                                               Tag="{Binding}"
                                               IsReadOnly="True">
                                            <TextBox.Style>
                                                <Style TargetType="TextBox">
                                                    <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CreditDays}" Value="0">
                                                            <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBox.Style>
                                        </TextBox>

                                        <TextBlock Grid.Column="2" Text="días" 
                                                 FontSize="11" 
                                                 Margin="0,0,8,0"
                                                 Foreground="{StaticResource TextGray}"
                                                 VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Acciones -->
                                <StackPanel Grid.Column="6" Orientation="Horizontal" 
                                          VerticalAlignment="Center">
                                    <!-- Botón Guardar (solo visible cuando hay cambios) -->
                                    <Button Style="{StaticResource IconButton}"
                                            Click="SaveButton_Click"
                                            Tag="{Binding}"
                                            ToolTip="Guardar cambios"
                                            Visibility="{Binding HasChanges, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <TextBlock Text="💾" FontSize="16" Foreground="{StaticResource SuccessColor}"/>
                                    </Button>

                                    <Button Style="{StaticResource IconButton}"
                                            Click="EditButton_Click"
                                            Tag="{Binding}"
                                            ToolTip="Editar todos los campos">
                                        <TextBlock Text="✏️" FontSize="16"/>
                                    </Button>

                                    <Button Style="{StaticResource IconButton}"
                                            Click="ToggleActiveButton_Click"
                                            Tag="{Binding}"
                                            Margin="4,0">
                                        <TextBlock FontSize="16">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="⏸️"/>
                                                    <Setter Property="ToolTip" Value="Desactivar"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Text" Value="▶️"/>
                                                            <Setter Property="ToolTip" Value="Activar"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Button>

                                    <Button Style="{StaticResource IconButton}"
                                            Click="DeleteButton_Click"
                                            Tag="{Binding}"
                                            ToolTip="Eliminar proveedor">
                                        <TextBlock Text="🗑️" FontSize="16" Foreground="{StaticResource DangerColor}"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Mensaje cuando no hay proveedores -->
        <Border Grid.Row="2" 
                Name="EmptyStatePanel"
                Visibility="Collapsed"
                Margin="24,40"
                Padding="40"
                Background="White"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="📦" FontSize="48" 
                         HorizontalAlignment="Center"
                         Margin="0,0,0,16"/>
                <TextBlock Text="No hay proveedores registrados" 
                         FontSize="18" FontWeight="SemiBold"
                         HorizontalAlignment="Center"
                         Foreground="{StaticResource DarkText}"
                         Margin="0,0,0,8"/>
                <TextBlock Text="Agrega tu primer proveedor para comenzar" 
                         FontSize="14"
                         HorizontalAlignment="Center"
                         Foreground="{StaticResource TextGray}"
                         Margin="0,0,0,16"/>
                <Button Content="➕ Agregar Proveedor" 
                        Style="{StaticResource ModernButton}"
                        HorizontalAlignment="Center"
                        Click="NewSupplierButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Barra de estado -->
        <Border Grid.Row="3" Background="White" BorderThickness="0,1,0,0" 
                BorderBrush="{StaticResource BorderGray}" Padding="24,10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Name="StatusText" Text="Listo" FontSize="12"
                             FontWeight="SemiBold" Foreground="{StaticResource SuccessColor}"
                             Margin="0,0,16,0"/>
                    <TextBlock Name="CountText" Text="0 proveedores" 
                             FontSize="11" Foreground="{StaticResource TextGray}"/>
                </StackPanel>

                <TextBlock Text="💡 Tip: Haz clic en cualquier campo para editarlo (ENTER guarda, ESC cancela)" 
                         FontSize="11" Foreground="{StaticResource TextGray}"
                         HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Window>