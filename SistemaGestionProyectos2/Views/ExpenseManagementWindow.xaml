<Window x:Class="SistemaGestionProyectos2.Views.ExpenseManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:system="clr-namespace:System;assembly=mscorlib"
        Title="Portal de Proveedores - Sistema de Gestión" 
        Height="900" 
        Width="1500"
        WindowStartupLocation="CenterScreen"
        WindowState="Normal"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Colores consistentes con el proyecto -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#6366F1"/>
        <SolidColorBrush x:Key="PrimaryLight" Color="#E0E7FF"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#10B981"/>
        <SolidColorBrush x:Key="SuccessLight" Color="#D1FAE5"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="DangerLight" Color="#FEE2E2"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="WarningLight" Color="#FEF3C7"/>
        <SolidColorBrush x:Key="InfoColor" Color="#3B82F6"/>
        <SolidColorBrush x:Key="InfoLight" Color="#DBEAFE"/>
        <SolidColorBrush x:Key="BackgroundGray" Color="#F9FAFB"/>
        <SolidColorBrush x:Key="BorderGray" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="TextGray" Color="#6B7280"/>
        <SolidColorBrush x:Key="DarkText" Color="#111827"/>

        <!-- Estilo para botones modernos -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="6" Direction="270" 
                                                ShadowDepth="1" Opacity="0.1"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para botones secundarios -->
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundGray}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para DataGrid consistente -->
        <Style x:Key="ModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="52"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource BackgroundGray}"/>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                        <Style.Triggers>
                            <!-- Fila nueva -->
                            <DataTrigger Binding="{Binding IsNew}" Value="True">
                                <Setter Property="Background" Value="{StaticResource SuccessLight}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource SuccessColor}"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </DataTrigger>
                            <!-- Pendiente -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Status}" Value="PENDIENTE"/>
                                    <Condition Binding="{Binding IsNew}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" Value="{StaticResource WarningLight}"/>
                            </MultiDataTrigger>
                            <!-- Vencido -->
                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                <Setter Property="Background" Value="{StaticResource DangerLight}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </DataTrigger>
                            <!-- Hover -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <!-- Seleccionado -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryLight}"/>
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo para badges de estado -->
        <Style x:Key="StatusBadge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="10,4"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource BackgroundGray}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header compacto -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderGray}" 
                BorderThickness="0,0,0,1">
            <Grid Margin="24,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Vertical">
                    <TextBlock Text="PORTAL DE PROVEEDORES" 
                             FontSize="20" 
                             FontWeight="Bold" 
                             Foreground="{StaticResource DarkText}"/>
                    <TextBlock Text="Gestión de Gastos y Pagos" 
                             FontSize="12" 
                             Foreground="{StaticResource TextGray}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Name="BackButton"
                        Click="BackButton_Click"
                        Style="{StaticResource SecondaryButton}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="←" FontSize="14" Margin="0,0,6,0"/>
                        <TextBlock Text="Volver" FontSize="13"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Proveedor filtrado destacado (oculto por defecto) -->
        <Border Grid.Row="1" Name="FilteredSupplierPanel"
                Background="{StaticResource InfoLight}"
                Padding="24,16"
                Visibility="Collapsed">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🏢" FontSize="28" Margin="0,0,12,0"
                             Foreground="{StaticResource InfoColor}"
                             VerticalAlignment="Center"/>
                    <TextBlock Name="FilteredSupplierNameText" 
                             Text="[Nombre del Proveedor]" 
                             FontSize="24" FontWeight="Bold"
                             Foreground="{StaticResource InfoColor}"
                             VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="✕ Quitar filtro"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="{StaticResource InfoColor}"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="12,6"
                        FontSize="13"
                        Cursor="Hand"
                        Click="ClearSupplierFilter_Click"/>
            </Grid>
        </Border>

        <!-- Tarjetas de resumen -->
        <Grid Grid.Row="2" Margin="24,12,24,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total General -->
            <Border Margin="0,0,6,0" Background="White" CornerRadius="8" Padding="16,12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="TOTAL GENERAL" FontSize="10" FontWeight="SemiBold"
                             Foreground="{StaticResource TextGray}" Margin="0,0,0,4"/>
                    <TextBlock Name="TotalGeneralText" Text="$0.00" FontSize="20" 
                             FontWeight="Bold" Foreground="{StaticResource DarkText}"
                             HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <!-- Pendiente -->
            <Border Margin="6,0" Background="White" CornerRadius="8" Padding="16,12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="PENDIENTE" FontSize="10" FontWeight="SemiBold"
                             Foreground="{StaticResource TextGray}" Margin="0,0,0,4"/>
                    <TextBlock Name="TotalPendienteText" Text="$0.00" FontSize="20" 
                             FontWeight="Bold" Foreground="{StaticResource WarningColor}"
                             HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <!-- Pagado -->
            <Border Margin="6,0" Background="White" CornerRadius="8" Padding="16,12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="PAGADO" FontSize="10" FontWeight="SemiBold"
                             Foreground="{StaticResource TextGray}" Margin="0,0,0,4"/>
                    <TextBlock Name="TotalPagadoText" Text="$0.00" FontSize="20" 
                             FontWeight="Bold" Foreground="{StaticResource SuccessColor}"
                             HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>

            <!-- Registros
            <Border Margin="6,0,0,0" Background="White" CornerRadius="8" Padding="16,12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" Direction="270" ShadowDepth="1" Opacity="0.05"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="REGISTROS" FontSize="10" FontWeight="SemiBold"
                             Foreground="{StaticResource TextGray}" Margin="0,0,0,4"/>
                    <TextBlock Name="RecordCountText" Text="0" FontSize="20" 
                             FontWeight="Bold" Foreground="{StaticResource PrimaryColor}"
                             HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
            -->
        </Grid>

        <!-- Barra de herramientas -->
        <Grid Grid.Row="3" Margin="24,0,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Filtros izquierda -->
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <!-- Búsqueda -->
                <Border Background="White" CornerRadius="6" Width="240" 
                        BorderThickness="1" BorderBrush="{StaticResource BorderGray}">
                    <Grid>
                        <TextBox Name="SearchBox" BorderThickness="0"
                               Background="Transparent" Padding="32,8,8,8"
                               FontSize="13" TextChanged="SearchBox_TextChanged"
                               VerticalAlignment="Center"/>
                        <TextBlock Text="🔍" Margin="10,0,0,0" VerticalAlignment="Center"
                                 Foreground="{StaticResource TextGray}" IsHitTestVisible="False"/>
                        <TextBlock Text="Buscar..." Margin="32,0,0,0"
                                 VerticalAlignment="Center" Foreground="{StaticResource TextGray}"
                                 IsHitTestVisible="False">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Hidden"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Text.Length, ElementName=SearchBox}" Value="0">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- Filtro Proveedor -->
                <ComboBox Name="SupplierFilterCombo" Width="180" Height="34"
                        Margin="8,0" FontSize="13"
                        SelectionChanged="SupplierFilterCombo_SelectionChanged"/>

                <!-- Filtro Orden -->
                <ComboBox Name="OrderFilterCombo" Width="160" Height="34"
                        Margin="0,0,8,0" FontSize="13"
                        SelectionChanged="OrderFilterCombo_SelectionChanged"
                        DisplayMemberPath="Display"/>

                <!-- Filtro Estado -->
                <ComboBox Name="StatusFilterCombo" Width="120" Height="34"
                        FontSize="13" SelectionChanged="StatusFilterCombo_SelectionChanged">
                    <ComboBoxItem>Todos</ComboBoxItem>
                    <ComboBoxItem>PENDIENTE</ComboBoxItem>
                    <ComboBoxItem>PAGADO</ComboBoxItem>
                </ComboBox>
            </StackPanel>

            <!-- Botones derecha -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Name="RefreshButton" Style="{StaticResource SecondaryButton}"
                        Click="RefreshButton_Click" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🔄" FontSize="13" Margin="0,0,6,0"/>
                        <TextBlock Text="Actualizar"/>
                    </StackPanel>
                </Button>

                <Button Name="SuppliersButton" Style="{StaticResource SecondaryButton}"
                        Click="SuppliersButton_Click" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👥" FontSize="13" Margin="0,0,6,0"/>
                        <TextBlock Text="Proveedores"/>
                    </StackPanel>
                </Button>

                <Button Name="NewExpenseButton" Style="{StaticResource ModernButton}"
                        Click="NewExpenseButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" FontSize="13" Margin="0,0,6,0"/>
                        <TextBlock Text="Nuevo Gasto"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- DataGrid principal -->
        <Border Grid.Row="4" Margin="24,0,24,12" Background="White" 
                CornerRadius="12" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Direction="270" ShadowDepth="2" Opacity="0.08"/>
            </Border.Effect>

            <DataGrid Name="ExpensesDataGrid"
                    Style="{StaticResource ModernDataGrid}"
                    SelectionChanged="ExpensesDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <!-- Proveedor -->
                    <DataGridTemplateColumn Header="PROVEEDOR" Width="1.8*" CanUserSort="True"
                                          SortMemberPath="SupplierName">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SupplierName}"
                                         VerticalAlignment="Center" Padding="12,0"
                                         FontSize="14"
                                         Cursor="Hand"
                                         MouseLeftButtonDown="SupplierName_MouseDown"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox Name="SupplierCombo"
                                        SelectedValue="{Binding SupplierId}"
                                        SelectedValuePath="Id"
                                        DisplayMemberPath="SupplierName"
                                        Loaded="SupplierCombo_Loaded"
                                        SelectionChanged="SupplierCombo_SelectionChanged"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Descripción -->
                    <DataGridTextColumn Header="DESCRIPCIÓN" Width="2.5*"
                                      Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                                      CanUserSort="True" SortMemberPath="Description">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Padding" Value="12,0"/>
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Orden -->
                    <DataGridTemplateColumn Header="ORDEN" Width="140" CanUserSort="True"
                                          SortMemberPath="OrderNumber">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OrderNumber}"
                                         VerticalAlignment="Center" HorizontalAlignment="Center"
                                         FontSize="13" Padding="8,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox SelectedValue="{Binding OrderId}"
                                        Loaded="OrderCombo_Loaded"
                                        SelectionChanged="OrderCombo_SelectionChanged"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Fecha Compra -->
                    <DataGridTemplateColumn Header="F. COMPRA" Width="100" CanUserSort="True"
                                          SortMemberPath="ExpenseDate">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding ExpenseDateDisplay}"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"
                                         FontSize="13"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <DatePicker SelectedDate="{Binding ExpenseDate}"
                                          VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Total (alineado a la derecha) -->
                    <DataGridTemplateColumn Header="TOTAL" Width="120" CanUserSort="True"
                                          SortMemberPath="TotalExpense">
                        <DataGridTemplateColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader" 
                                 BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                            </Style>
                        </DataGridTemplateColumn.HeaderStyle>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalExpenseDisplay}"
                                         FontWeight="SemiBold" FontSize="14"
                                         HorizontalAlignment="Right" VerticalAlignment="Center"
                                         Margin="0,0,12,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding TotalExpense, StringFormat=F2}"
                                       TextAlignment="Right" Padding="8,4"
                                       PreviewTextInput="TotalAmount_PreviewTextInput"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Estado -->
                    <DataGridTemplateColumn Header="ESTADO" Width="100" IsReadOnly="True"
                                          CanUserSort="True" SortMemberPath="Status">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource StatusBadge}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="PAGADO">
                                                    <Setter Property="Background" Value="{StaticResource SuccessLight}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="PENDIENTE">
                                                    <Setter Property="Background" Value="{StaticResource WarningLight}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="PAGADO">
                                                        <Setter Property="Foreground" Value="{StaticResource SuccessColor}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="PENDIENTE">
                                                        <Setter Property="Foreground" Value="{StaticResource WarningColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Fecha Pago -->
                    <DataGridTemplateColumn Header="F. PAGO" Width="100" CanUserSort="True"
                                          SortMemberPath="PaidDate">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PaidDateDisplay}"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"
                                         FontSize="13">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsPaid}" Value="False">
                                                    <Setter Property="Cursor" Value="Hand"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
                                                    <Setter Property="FontStyle" Value="Italic"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <DatePicker SelectedDate="{Binding PaidDate}"
                                          SelectedDateChanged="PaidDatePicker_Changed"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Método Pago -->
                    <DataGridTemplateColumn Header="MÉTODO" Width="120" CanUserSort="True"
                                          SortMemberPath="PayMethod">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding PayMethod}"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"
                                         FontSize="13"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate>
                                <ComboBox SelectedItem="{Binding PayMethod}"
                                        SelectionChanged="PayMethodCombo_Changed">
                                    <ComboBox.Items>
                                        <system:String>TRANSFERENCIA</system:String>
                                        <system:String>EFECTIVO</system:String>
                                        <system:String>CHEQUE</system:String>
                                        <system:String>CRÉDITO</system:String>
                                        <system:String>DÉBITO</system:String>
                                    </ComboBox.Items>
                                </ComboBox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>

                    <!-- Acciones -->
                    <DataGridTemplateColumn Header="ACCIONES" Width="120" CanUserSort="False">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <!-- Botón Guardar (solo nuevos) -->
                                    <Button Content="💾" Tag="{Binding}" Click="SaveNewButton_Click"
                                          Width="28" Height="28" Margin="2,0"
                                          Background="{StaticResource SuccessColor}"
                                          Foreground="White" BorderThickness="0"
                                          ToolTip="Guardar" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                  CornerRadius="4" Padding="4">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Botón Pagar -->
                                    <Button Content="💵" Tag="{Binding}" Click="PayButton_Click"
                                          Width="28" Height="28" Margin="2,0"
                                          Background="{StaticResource SuccessColor}"
                                          Foreground="White" BorderThickness="0"
                                          ToolTip="Registrar Pago" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                  CornerRadius="4" Padding="4">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition Binding="{Binding IsPaid}" Value="False"/>
                                                            <Condition Binding="{Binding IsNew}" Value="False"/>
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Botón Editar -->
                                    <Button Content="✏️" Tag="{Binding}" Click="EditButton_Click"
                                          Width="28" Height="28" Margin="2,0"
                                          Background="{StaticResource PrimaryColor}"
                                          Foreground="White" BorderThickness="0"
                                          ToolTip="Editar" Cursor="Hand">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Visibility" Value="Visible"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                  CornerRadius="4" Padding="4">
                                                                <ContentPresenter HorizontalAlignment="Center"
                                                                                VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsNew}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                    <!-- Botón Eliminar -->
                                    <Button Content="🗑️" Tag="{Binding}" Click="DeleteButton_Click"
                                          Width="28" Height="28" Margin="2,0"
                                          Background="{StaticResource DangerColor}"
                                          Foreground="White" BorderThickness="0"
                                          ToolTip="Eliminar" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                      CornerRadius="4" Padding="4">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Barra de estado -->
        <Border Grid.Row="5" Background="White" BorderThickness="0,1,0,0" 
                BorderBrush="{StaticResource BorderGray}" Padding="24,10">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Name="StatusText" Text="Listo" FontSize="12"
                             FontWeight="SemiBold" Foreground="{StaticResource SuccessColor}"
                             Margin="0,0,16,0"/>
                    <TextBlock Name="LastUpdateText" Text="Última actualización: --"
                             FontSize="11" Foreground="{StaticResource TextGray}"/>
                </StackPanel>

                <TextBlock Name="CurrentUserText" Text="Usuario: --"
                         FontSize="11" Foreground="{StaticResource TextGray}"
                         HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Window>