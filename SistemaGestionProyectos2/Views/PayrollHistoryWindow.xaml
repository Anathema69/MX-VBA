<Window x:Class="SistemaGestionProyectos2.Views.PayrollHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Historial de Cambios - Nómina" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Estilos consistentes -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#5B3FF9"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#48BB78"/>
        <SolidColorBrush x:Key="WarningColor" Color="#F59E0B"/>
        <SolidColorBrush x:Key="DangerColor" Color="#EF4444"/>
        <SolidColorBrush x:Key="BackgroundGray" Color="#F7FAFC"/>
        <SolidColorBrush x:Key="BorderGray" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="TextGray" Color="#718096"/>
        <SolidColorBrush x:Key="DarkText" Color="#2D3748"/>
    </Window.Resources>

    <Border Background="White" CornerRadius="12" BorderThickness="1" 
            BorderBrush="{StaticResource BorderGray}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" 
                    CornerRadius="12,12,0,0" Padding="25,20">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊 " FontSize="24" Foreground="White" 
                                 VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Name="HeaderTitle" Text="HISTORIAL DE CAMBIOS" 
                                     FontSize="20" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Name="SubtitleText" Text="Todos los empleados" 
                                     FontSize="13" Foreground="White" Opacity="0.9"/>
                        </StackPanel>
                    </StackPanel>
                    <Button Name="CloseButton" Click="CloseButton_Click"
                            HorizontalAlignment="Right"
                            Background="Transparent" BorderThickness="0"
                            Foreground="White" FontSize="20" Cursor="Hand">
                        <TextBlock Text="✕"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Nota informativa para MVP 
            <Border Grid.Row="1" Background="#FEF3C7" Padding="15" 
                    BorderBrush="#FCD34D" BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="💡" FontSize="16" Margin="0,0,10,0" 
                             Foreground="#92400E"/>
                    <TextBlock Foreground="#92400E" FontSize="13">
                        <Run Text="Versión MVP:" FontWeight="Bold"/>
                        <Run Text="Los cambios se aplican inmediatamente. En la versión completa podrás programar cambios futuros y aplicar ajustes retroactivos."/>
                    </TextBlock>
                </StackPanel>
            </Border>
            -->

            <!-- DataGrid de historial -->
            <DataGrid Grid.Row="2" Name="HistoryDataGrid"
                    AutoGenerateColumns="False"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    GridLinesVisibility="None"
                    BorderThickness="0"
                    Background="White"
                    RowHeight="50"
                    HeadersVisibility="Column"
                    Margin="20,20,20,10">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource BackgroundGray}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextGray}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="12"/>
                        <Setter Property="Padding" Value="10,12"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="FECHA" 
                                      Binding="{Binding EffectiveDate, StringFormat={}{0:dd/MM/yyyy}}" 
                                      Width="100"/>

                    <DataGridTextColumn Header="EMPLEADO" 
                                      Binding="{Binding Employee}" 
                                      Width="200"/>

                    <!-- Reemplazar el DataGridTemplateColumn de TIPO DE CAMBIO con este -->
                    <DataGridTemplateColumn Header="TIPO DE CAMBIO" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="4" Padding="8,4" Margin="2"
                    HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding ChangeType}" Value="SALARY_CHANGE">
                                                    <Setter Property="Background" Value="#E0E7FF"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ChangeType}" Value="PROMOTION">
                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ChangeType}" Value="BENEFIT_UPDATE">
                                                    <Setter Property="Background" Value="#DBEAFE"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ChangeType}" Value="NEW_HIRE">
                                                    <Setter Property="Background" Value="#D1FAE5"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding ChangeType}" Value="TERMINATION">
                                                    <Setter Property="Background" Value="#FEE2E2"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                            <Setter Property="Background" Value="#F3F4F6"/>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock FontSize="11" FontWeight="SemiBold"
                         HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="SALARY_CHANGE">
                                                        <Setter Property="Text" Value="SALARIO"/>
                                                        <Setter Property="Foreground" Value="#5B3FF9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="PROMOTION">
                                                        <Setter Property="Text" Value="PROMOCIÓN"/>
                                                        <Setter Property="Foreground" Value="#10B981"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="BENEFIT_UPDATE">
                                                        <Setter Property="Text" Value="PRESTACIONES"/>
                                                        <Setter Property="Foreground" Value="#3B82F6"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="NEW_HIRE">
                                                        <Setter Property="Text" Value="ALTA"/>
                                                        <Setter Property="Foreground" Value="#10B981"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding ChangeType}" Value="TERMINATION">
                                                        <Setter Property="Text" Value="BAJA"/>
                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                                <Setter Property="Text" Value="CAMBIO"/>
                                                <Setter Property="Foreground" Value="#6B7280"/>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="RESUMEN DE CAMBIOS" 
                                      Binding="{Binding ChangeSummary}" 
                                      Width="*"/>


                    <!-- Columna de NÓMINA NUEVA -->
                    <DataGridTextColumn Header="NÓMINA NUEVA" 
                  Binding="{Binding MonthlyPayroll, StringFormat={}{0:C}, ConverterCulture=es-MX}" 
                  Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
                                <Setter Property="TextAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Footer con estadísticas -->
            <Border Grid.Row="3" Background="{StaticResource BackgroundGray}" 
                    Padding="25,15" CornerRadius="0,0,12,12">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Total de cambios: " FontSize="13" 
                                 Foreground="{StaticResource TextGray}"/>
                        <TextBlock Name="TotalChangesText" Text="0" FontSize="13" 
                                 FontWeight="Bold" Foreground="{StaticResource DarkText}"
                                 Margin="0,0,20,0"/>

                        <TextBlock Text="Último cambio: " FontSize="13" 
                                 Foreground="{StaticResource TextGray}"/>
                        <TextBlock Name="LastChangeText" Text="--" FontSize="13" 
                                 Foreground="{StaticResource DarkText}"/>
                    </StackPanel>

                    <Button Name="ExportButton" Click="ExportButton_Click"
                            HorizontalAlignment="Right"
                            Background="{StaticResource SuccessColor}"
                            Foreground="White" BorderThickness="0"
                            Padding="15,8" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="6"
                                        Padding="{TemplateBinding Padding}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="📥 " FontSize="14" Margin="0,0,5,0"/>
                                        <TextBlock Text="Exportar" FontSize="13" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>