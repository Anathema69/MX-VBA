<!-- Archivo: Views/InvoiceManagementWindow.xaml - VERSIÓN MEJORADA -->
<Window x:Class="SistemaGestionProyectos2.Views.InvoiceManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gestión de Facturas - IMA Mecatrónica" 
        Height="750" 
        Width="1200"
        WindowStartupLocation="CenterOwner"
        WindowState="Normal"
        WindowStyle="None"
        ResizeMode="NoResize"
        MinHeight="600"
        MinWidth="1000">

    <Window.Resources>
        <!-- Estilos mejorados y consistentes -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0D47A1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Negro con gradiente sutil -->
        <Border Grid.Row="0" Height="60">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#1a1a1a" Offset="0"/>
                    <GradientStop Color="#2d2d2d" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <!-- Título a la izquierda -->
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Margin="20,0,0,0">
                    <Border Background="#FFC107"
                            Width="40" Height="40"
                            CornerRadius="20"
                            Margin="0,0,15,0">
                        <TextBlock Text="💰"
                                 FontSize="22"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="GESTIÓN DE FACTURAS"
                             Foreground="White"
                             FontSize="22"
                             FontWeight="Light"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Botón Volver a la derecha (responsivo) -->
                <Button Content="⬅ Volver"
                        Click="BackButton_Click"
                        Background="Transparent"
                        BorderBrush="White"
                        BorderThickness="1"
                        Foreground="White"
                        Padding="15,5"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="0,0,20,0"
                        Cursor="Hand"/>
            </Grid>
        </Border>

        <!-- Información de la Orden con diseño mejorado y NUEVOS CAMPOS -->
        <Border Grid.Row="1" Padding="25,20">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#1565C0" Offset="0"/>
                    <GradientStop Color="#1976D2" Offset="0.5"/>
                    <GradientStop Color="#1E88E5" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Primera línea: Info básica -->
                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Border Background="#00000020" CornerRadius="15" Padding="12,6" Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ORDEN" 
                                     Foreground="White"
                                     FontSize="13"
                                     Opacity="0.9"/>
                            <TextBlock x:Name="OrderNumberText"
                                     Text=" HJ_0001"
                                     Foreground="#FFC107"
                                     FontSize="16"
                                     FontWeight="Bold"
                                     Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#00000020" CornerRadius="15" Padding="12,6" Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="CLIENTE" 
                                     Foreground="White"
                                     FontSize="13"
                                     Opacity="0.9"/>
                            <TextBlock x:Name="ClientNameText"
                                     Text=" Android"
                                     Foreground="White"
                                     FontSize="16"
                                     FontWeight="SemiBold"
                                     Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#00000020" CornerRadius="15" Padding="12,6">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="MONTO ORDEN" 
                                     Foreground="White"
                                     FontSize="13"
                                     Opacity="0.9"/>
                            <TextBlock x:Name="OrderTotalText"
                                     Text=" $58,000.00"
                                     Foreground="#4CAF50"
                                     FontSize="16"
                                     FontWeight="Bold"
                                     Margin="8,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Segunda línea: Balance mejorado con NUEVO CAMPO -->
                <Grid Grid.Row="1" Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <StackPanel Margin="0,0,25,0">
                            <TextBlock Text="FACTURADO"
                                     Foreground="#B3E5FC"
                                     FontSize="11"
                                     FontWeight="Light"/>
                            <TextBlock x:Name="InvoicedAmountText"
                                     Text="$15,000.00"
                                     Foreground="White"
                                     FontSize="18"
                                     FontWeight="Bold"/>
                        </StackPanel>

                        <!-- NUEVO: Por Facturar sin IVA -->
                        <StackPanel Margin="0,0,25,0">
                            <TextBlock Text="POR FACTURAR (SIN IVA)"
                                     Foreground="#FFE082"
                                     FontSize="11"
                                     FontWeight="Light"/>
                            <TextBlock x:Name="BalanceWithoutTaxText"
                                     Text="$37,068.97"
                                     Foreground="#FFEB3B"
                                     FontSize="18"
                                     FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="POR FACTURAR (CON IVA)"
                                     Foreground="#B3E5FC"
                                     FontSize="11"
                                     FontWeight="Light"/>
                            <TextBlock x:Name="BalanceText"
                                     Text="$43,000.00"
                                     Foreground="#FFE082"
                                     FontSize="18"
                                     FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Barra de Progreso Mejorada -->
                    <Grid Grid.Column="1" Margin="30,5,30,0">
                        <Border Background="#00000030" 
                                CornerRadius="10" 
                                Height="22">
                            <Grid>
                                <ProgressBar x:Name="InvoiceProgressBar"
                                           Height="22"
                                           Maximum="100"
                                           Value="26"
                                           BorderThickness="0"
                                           BorderBrush="Transparent">
                                    <ProgressBar.Background>
                                        <SolidColorBrush Color="Transparent"/>
                                    </ProgressBar.Background>
                                    <ProgressBar.Foreground>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#4CAF50" Offset="0"/>
                                            <GradientStop Color="#8BC34A" Offset="1"/>
                                        </LinearGradientBrush>
                                    </ProgressBar.Foreground>
                                    <ProgressBar.Style>
                                        <Style TargetType="ProgressBar">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="ProgressBar">
                                                        <Grid>
                                                            <Border Background="#00000030" 
                                                                    CornerRadius="10"/>
                                                            <Border x:Name="PART_Track" 
                                                                    Background="Transparent"
                                                                    CornerRadius="10"/>
                                                            <Border x:Name="PART_Indicator"
                                                                    Background="{TemplateBinding Foreground}"
                                                                    CornerRadius="10"
                                                                    HorizontalAlignment="Left"/>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </ProgressBar.Style>
                                </ProgressBar>
                                <TextBlock x:Name="ProgressPercentText"
                                         Text="26%"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"
                                         Foreground="White"
                                         FontWeight="Bold"
                                         FontSize="12">
                                    <TextBlock.Effect>
                                        <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </Grid>

                    <Border Grid.Column="2"
                            Background="#00000020" 
                            CornerRadius="10" 
                            Padding="12,6">
                        <TextBlock x:Name="WarningText"
                                 Text="⚠️ Puede facturar hasta $43,000.00 más"
                                 Foreground="#FFE082"
                                 FontSize="13"
                                 VerticalAlignment="Center"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>

        <!-- Toolbar de Acciones con mejor diseño y NUEVO BOTÓN -->
        <Border Grid.Row="2" 
                Background="#FAFAFA"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,1"
                Padding="15,12">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <!-- Botón Nueva Factura -->
                    <Button x:Name="AddInvoiceButton"
                           Click="AddInvoiceButton_Click"
                           Style="{StaticResource ModernButton}"
                           Background="#4CAF50"
                           Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Border Background="White" 
                                    Width="20" Height="20" 
                                    CornerRadius="10"
                                    Margin="0,0,8,0">
                                <TextBlock Text="+" 
                                         Foreground="#4CAF50"
                                         FontSize="14"
                                         FontWeight="Bold"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="Nueva Factura" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- NUEVO: Botón Completar Factura -->
                    <Button x:Name="CompleteInvoiceButton"
                           Click="CompleteInvoiceButton_Click"
                           Style="{StaticResource ModernButton}"
                           Background="#FF9800"
                           Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚡" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Completar Factura" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="SaveAllButton"
                           Click="SaveAllButton_Click"
                           Style="{StaticResource ModernButton}"
                           Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" FontSize="16" Margin="0,0,8,0"/>
                            <TextBlock Text="Guardar Todo" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="RefreshButton"
                           Click="RefreshButton_Click"
                           Width="38"
                           Height="38"
                           Background="White"
                           BorderBrush="#BDBDBD"
                           BorderThickness="1"
                           Cursor="Hand"
                           ToolTip="Actualizar datos">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                       Background="{TemplateBinding Background}"
                                       BorderBrush="{TemplateBinding BorderBrush}"
                                       BorderThickness="{TemplateBinding BorderThickness}"
                                       CornerRadius="5">
                                    <TextBlock Text="🔄" 
                                             FontSize="16"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <Border HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Background="#E3F2FD"
                       CornerRadius="15"
                       Padding="15,8">
                    <TextBlock x:Name="StatusMessage"
                             Text="Listo para agregar facturas"
                             FontStyle="Italic"
                             Foreground="#1976D2"
                             FontSize="12"/>
                </Border>
            </Grid>
        </Border>

        <!-- DataGrid con estilos mejorados y consistentes -->
        <Border Grid.Row="3" 
                Margin="15" 
                Background="White"
                CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="2" 
                                Direction="270" 
                                Color="#000000" 
                                Opacity="0.1" 
                                BlurRadius="10"/>
            </Border.Effect>

            <Grid>
                <DataGrid x:Name="InvoicesDataGrid"
                         AutoGenerateColumns="False"
                         CanUserAddRows="False"
                         CanUserDeleteRows="False"
                         CanUserResizeColumns="True"
                         CanUserReorderColumns="False"
                         SelectionMode="Single"
                         GridLinesVisibility="None"
                         HeadersVisibility="Column"
                         Background="Transparent"
                         BorderThickness="0"
                         RowHeight="45"
                         FontSize="13"
                         CellEditEnding="InvoicesDataGrid_CellEditEnding"
                         PreparingCellForEdit="InvoicesDataGrid_PreparingCellForEdit"
                         PreviewKeyDown="InvoicesDataGrid_PreviewKeyDown"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Hidden">

                    <DataGrid.Resources>
                        <!-- Header Style -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#37474F" Offset="0"/>
                                        <GradientStop Color="#263238" Offset="1"/>
                                    </LinearGradientBrush>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Height" Value="42"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="BorderBrush" Value="#455A64"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>

                        <!-- Row Style con hover mejorado -->
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="BorderBrush" Value="#F5F5F5"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F5F5F5"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#E3F2FD"/>
                                    <Setter Property="BorderBrush" Value="#2196F3"/>
                                    <Setter Property="BorderThickness" Value="2,0,0,1"/>
                                </Trigger>
                                <DataTrigger Binding="{Binding IsNew}" Value="True">
                                    <Setter Property="Background" Value="#FFF9C4"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HasChanges}" Value="True">
                                    <Setter Property="FontStyle" Value="Italic"/>
                                    <Setter Property="BorderBrush" Value="#FF9800"/>
                                    <Setter Property="BorderThickness" Value="2,0,0,1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>

                        <!-- Cell Style -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="8,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <!-- Número con indicador de nuevo -->
                        <DataGridTemplateColumn Header="No." Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid>
                                        <TextBlock Text="{Binding Id}" 
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"
                                                 FontWeight="SemiBold"/>
                                        <Border x:Name="NewIndicator"
                                               Width="8" Height="8"
                                               Background="#FFC107"
                                               CornerRadius="4"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Top"
                                               Margin="0,5,5,0"
                                               Visibility="Collapsed"
                                               ToolTip="Nueva factura"/>
                                    </Grid>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IsNew}" Value="True">
                                            <Setter TargetName="NewIndicator" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Folio con alineación consistente -->
                        <DataGridTextColumn Header="FOLIO FACTURA" 
                                           Binding="{Binding Folio}" 
                                           Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <!-- Fecha Factura -->
                        <DataGridTemplateColumn Header="FECHA FACTURA" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding InvoiceDate, StringFormat=dd/MM/yyyy}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding InvoiceDate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- Subtotal con alineación a la derecha -->
                        <DataGridTextColumn Header="SUBTOTAL" 
                                           Binding="{Binding Subtotal, StringFormat=C}" 
                                           Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#424242"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                            <DataGridTextColumn.EditingElementStyle>
                                <Style TargetType="TextBox">
                                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="TextAlignment" Value="Right"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.EditingElementStyle>
                        </DataGridTextColumn>

                        <!-- Total con IVA -->
                        <DataGridTextColumn Header="TOTAL (c/IVA)" 
                                           Binding="{Binding Total, StringFormat=C}" 
                                           Width="130"
                                           IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#2E7D32"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Margin" Value="0,0,10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Fecha Recepción -->
                        <DataGridTemplateColumn Header="RECIBO FACT." Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ReceptionDate, StringFormat=dd/MM/yyyy}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding ReceptionDate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- Pago Programado -->
                        <DataGridTextColumn Header="PAGO PROG." 
                                           Binding="{Binding DueDate, StringFormat=dd/MM/yyyy}" 
                                           Width="110"
                                           IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontStyle" Value="Italic"/>
                                    <Setter Property="Foreground" Value="#757575"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Fecha Pago Real -->
                        <DataGridTemplateColumn Header="FECHA PAGO" Width="130">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PaymentDate, StringFormat=dd/MM/yyyy}"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                            <DataGridTemplateColumn.CellEditingTemplate>
                                <DataTemplate>
                                    <DatePicker SelectedDate="{Binding PaymentDate}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellEditingTemplate>
                        </DataGridTemplateColumn>

                        <!-- Estado con diseño mejorado -->
                        <DataGridTemplateColumn Header="ESTADO" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" 
                                           Padding="12,5"
                                           HorizontalAlignment="Center"
                                           Background="{Binding StatusColor}">
                                        <Border.Effect>
                                            <DropShadowEffect ShadowDepth="1" 
                                                            Color="#000000" 
                                                            Opacity="0.2" 
                                                            BlurRadius="3"/>
                                        </Border.Effect>
                                        <TextBlock Text="{Binding Status}" 
                                                 FontWeight="Bold"
                                                 FontSize="11"
                                                 Foreground="White"
                                                 HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Acciones -->
                        <DataGridTemplateColumn Header="ACCIONES" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="🗑️" 
                                           Click="DeleteButton_Click"
                                           Tag="{Binding}"
                                           Width="32"
                                           Height="32"
                                           Background="Transparent"
                                           BorderThickness="0"
                                           FontSize="16"
                                           Cursor="Hand"
                                           ToolTip="Eliminar factura"
                                           HorizontalAlignment="Center">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border"
                                                       Background="Transparent"
                                                       CornerRadius="5">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#FFEBEE"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Footer mejorado -->
        <Border Grid.Row="4">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#37474F" Offset="0"/>
                    <GradientStop Color="#263238" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="25,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Información izquierda -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Border Background="#00000030" 
                            CornerRadius="8" 
                            Padding="15,8" 
                            Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Total Facturas: "
                                     Foreground="#B0BEC5"
                                     FontSize="13"/>
                            <TextBlock x:Name="InvoiceCountText"
                                     Text="0"
                                     Foreground="#FFC107"
                                     FontWeight="Bold"
                                     FontSize="16"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#00000030" 
                            CornerRadius="8" 
                            Padding="15,8" 
                            Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Suma Subtotales: "
                                     Foreground="#B0BEC5"
                                     FontSize="13"/>
                            <TextBlock x:Name="SubtotalSumText"
                                     Text="$0.00"
                                     Foreground="White"
                                     FontWeight="Bold"
                                     FontSize="16"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#4CAF5030" 
                            CornerRadius="8" 
                            Padding="15,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Suma Totales: "
                                     Foreground="#B0BEC5"
                                     FontSize="13"/>
                            <TextBlock x:Name="TotalSumText"
                                     Text="$0.00"
                                     Foreground="#4CAF50"
                                     FontWeight="Bold"
                                     FontSize="18"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Timestamp derecha -->
                <TextBlock Grid.Column="2"
                         x:Name="LastUpdateText"
                         Text="Última actualización: --"
                         Foreground="#607D8B"
                         FontSize="11"
                         VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</Window>