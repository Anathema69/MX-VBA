<Window x:Class="SistemaGestionProyectos2.Views.ClientManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SistemaGestionProyectos2.Services"
        Title="Gestión de Clientes - IMA Mecatrónica" 
        Height="700" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Window.Resources>
        <local:AdminVisibilityConverter x:Key="AdminVisibilityConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <!-- Header Negro -->
        <Border Grid.Row="0" Background="Black" Height="60">
            <Grid>
                <StackPanel Orientation="Horizontal" 
                           VerticalAlignment="Center"
                           Margin="20,0">
                    <TextBlock Text="👥" 
                             Foreground="#2196F3"
                             FontSize="24"
                             Margin="0,0,15,0"/>
                    <TextBlock Text="GESTIÓN DE CLIENTES" 
                             Foreground="White"
                             FontSize="22"
                             FontWeight="Bold"
                             VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Right"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0">
                    <Button x:Name="BackButton"
                           Click="BackButton_Click"
                           Content="← Volver"
                           Background="Transparent"
                           BorderBrush="White"
                           BorderThickness="1"
                           Foreground="White"
                           Padding="15,5"
                           Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tabs para Clientes y Contactos -->
        <Border Grid.Row="1" Background="#f5f5f5" Padding="10,5">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="ClientsTab"
                            Content="CLIENTES"
                            IsChecked="True"
                            GroupName="MainTabs"
                            Margin="0,0,20,0"
                            FontWeight="Bold"
                            FontSize="14"
                            Checked="ClientsTab_Checked"/>
                <RadioButton x:Name="ContactsTab"
                            Content="CONTACTOS"
                            GroupName="MainTabs"
                            FontWeight="Bold"
                            FontSize="14"
                            Checked="ContactsTab_Checked"/>
            </StackPanel>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="2" Background="#e0e0e0" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Botones de acción -->
                <StackPanel x:Name="ClientsToolbar" 
                           Grid.Column="0" 
                           Orientation="Horizontal"
                           Visibility="Visible">
                    <Button x:Name="NewClientButton"
                           Click="NewClientButton_Click"
                           Height="40"
                           Width="150"
                           Margin="0,0,10,0"
                           Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                       Background="#FFC107"
                                       CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#FFB300"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="16" Margin="0,0,8,0" Foreground="Black" FontWeight="Bold"/>
                            <TextBlock Text="NUEVO CLIENTE" FontWeight="Bold" Foreground="Black"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="EditClientButton"
                           Click="EditClientButton_Click"
                           Height="40"
                           Width="120"
                           Margin="0,0,10,0"
                           IsEnabled="False"
                           Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                       Background="#4CAF50"
                                       CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#45A049"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✏" FontSize="14" Margin="0,0,8,0" Foreground="White"/>
                            <TextBlock Text="EDITAR" FontWeight="Bold" Foreground="White"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="RefreshButton"
                           Click="RefreshButton_Click"
                           Content="🔄"
                           Width="40"
                           Height="40"
                           FontSize="18"
                           Background="White"
                           BorderBrush="#999"
                           BorderThickness="1"
                           Cursor="Hand"
                           ToolTip="Actualizar"/>
                </StackPanel>

                <!-- Toolbar para Contactos -->
                <StackPanel x:Name="ContactsToolbar" 
                           Grid.Column="0" 
                           Orientation="Horizontal"
                           Visibility="Collapsed">
                    <Button x:Name="NewContactButton"
                           Click="NewContactButton_Click"
                           Height="40"
                           Width="170"
                           Margin="0,0,10,0"
                           Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                       Background="#FFC107"
                                       CornerRadius="3">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#FFB300"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="+" FontSize="16" Margin="0,0,8,0" Foreground="Black" FontWeight="Bold"/>
                            <TextBlock Text="NUEVO CONTACTO" FontWeight="Bold" Foreground="Black"/>
                        </StackPanel>
                    </Button>

                    <ComboBox x:Name="ClientFilterCombo"
                             Width="200"
                             Height="35"
                             Margin="0,0,10,0"
                             DisplayMemberPath="Name"
                             SelectedValuePath="Id"
                             SelectionChanged="ClientFilterCombo_SelectionChanged">
                        
                    </ComboBox>
                </StackPanel>

                <!-- Búsqueda -->
                <Border Grid.Column="1" 
                       BorderBrush="#999"
                       BorderThickness="1"
                       Background="White"
                       CornerRadius="3"
                       Height="35"
                       HorizontalAlignment="Right"
                       Width="300"
                       Margin="10,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox x:Name="SearchBox"
                                BorderThickness="0"
                                VerticalAlignment="Center"
                                Margin="10,0"
                                FontSize="14"
                                TextChanged="SearchBox_TextChanged">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="Buscar..." 
                                                                     Foreground="#999" 
                                                                     FontStyle="Italic"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>

                        <TextBlock Grid.Column="1" 
                                 Text="🔍"
                                 FontSize="16"
                                 VerticalAlignment="Center"
                                 Margin="0,0,10,0"
                                 Foreground="#666"/>
                    </Grid>
                </Border>

                <!-- Estadísticas -->
                <StackPanel Grid.Column="2" 
                           Orientation="Horizontal"
                           VerticalAlignment="Center">
                    <Border Background="#2196F3"
                           CornerRadius="3"
                           Padding="10,5"
                           Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Total: "
                                     Foreground="White"
                                     FontSize="12"/>
                            <TextBlock x:Name="TotalCountText"
                                     Text="0"
                                     Foreground="White"
                                     FontWeight="Bold"
                                     FontSize="14"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid para Clientes -->
        <DataGrid x:Name="ClientsDataGrid"
                 Grid.Row="3"
                 Margin="10"
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 SelectionMode="Single"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 Background="White"
                 BorderThickness="1"
                 BorderBrush="#DDD"
                 RowHeight="40"
                 FontSize="12"
                 Visibility="Visible"
                 SelectionChanged="ClientsDataGrid_SelectionChanged"
                 MouseDoubleClick="ClientsDataGrid_MouseDoubleClick">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#f0f0f0"/>
                    <Setter Property="Foreground" Value="Black"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Height" Value="40"/>
                    <Setter Property="BorderBrush" Value="#DDD"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="10,0"/>
                </Style>

                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#BBDEFB"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                   Binding="{Binding Id}" 
                                   Width="50"/>

                <DataGridTextColumn Header="RAZÓN SOCIAL" 
                                   Binding="{Binding Name}" 
                                   Width="300"/>

                <DataGridTextColumn Header="RFC" 
                                   Binding="{Binding TaxId}" 
                                   Width="150"/>

                <DataGridTextColumn Header="DIRECCIÓN" 
                                   Binding="{Binding Address1}" 
                                   Width="*"/>

                <DataGridTextColumn Header="TELÉFONO" 
                                   Binding="{Binding Phone}" 
                                   Width="120"/>

                <DataGridTextColumn Header="DÍAS CRÉDITO" 
                                   Binding="{Binding Credit}" 
                                   Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="ESTADO" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border x:Name="StatusBorder"
                                   CornerRadius="3" 
                                   Padding="8,4"
                                   HorizontalAlignment="Center">
                                <TextBlock x:Name="StatusTextBlock"
                                         Text="{Binding StatusText}" 
                                         FontWeight="SemiBold"
                                         FontSize="11"
                                         HorizontalAlignment="Center"/>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                    <Setter TargetName="StatusBorder" Property="Background" Value="#C8E6C9"/>
                                    <Setter TargetName="StatusTextBlock" Property="Foreground" Value="#1B5E20"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                    <Setter TargetName="StatusBorder" Property="Background" Value="#FFCDD2"/>
                                    <Setter TargetName="StatusTextBlock" Property="Foreground" Value="#B71C1C"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="CONTACTOS" 
                                   Binding="{Binding ContactCount}" 
                                   Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- DataGrid para Contactos -->
        <DataGrid x:Name="ContactsDataGrid"
                 Grid.Row="3"
                 Margin="10"
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 SelectionMode="Single"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 Background="White"
                 BorderThickness="1"
                 BorderBrush="#DDD"
                 RowHeight="40"
                 FontSize="12"
                 Visibility="Collapsed">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                   Binding="{Binding Id}" 
                                   Width="50"/>

                <DataGridTextColumn Header="EMPRESA" 
                                   Binding="{Binding ClientName}" 
                                   Width="200"/>

                <DataGridTextColumn Header="NOMBRE CONTACTO" 
                                   Binding="{Binding ContactName}" 
                                   Width="250"/>

                <DataGridTextColumn Header="EMAIL" 
                                   Binding="{Binding Email}" 
                                   Width="250"/>

                <DataGridTextColumn Header="TELÉFONO" 
                                   Binding="{Binding Phone}" 
                                   Width="150"/>

                <DataGridTextColumn Header="CARGO" 
                                   Binding="{Binding Position}" 
                                   Width="150"/>

                <DataGridCheckBoxColumn Header="PRINCIPAL" 
                                       Binding="{Binding IsPrimary}" 
                                       Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="4" Background="#f0f0f0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Listo"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="LastUpdateText" Text=""/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>